//this explain the project is what but i want to take it is as a note and want to share what  the unknown words so i can easily understan the logic what is it.
1) First i learn about the lucide icon which can be import from the npm install lucide-react for the use of different icon.
For backend 

Intialize nodepackage manager 

npm init -y 
npm i cors express mongoose dotenv multer  and other required
npm i --save-dev nodemon


What is cors?
Cors is the cross-origin resource sharing 

browser block the request from differnt origin for security 


How different origin can be seperated.
1)Protocal
2)Port
3)Domain
Protocal ==>Rule that define how data are transferred and receive over the internet.
http://localhost:3000;
https://localhost:3000; have differnt origin;
Port http://localhost :30000 and http://localhost:5137
Domain:+=




Okay now we have to use the middleware called ap.use(express.json()) because we send the raw data(string) from the frontend without parsing the data cannot be understand by the expressw




we have created the differnet fn for the following follwrer out other job is to create the api routes or http endpoint

//nodejs fully support the top level await.